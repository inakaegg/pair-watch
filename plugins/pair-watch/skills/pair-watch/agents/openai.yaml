interface:
  display_name: "Pair-watch"
  short_description: "Two-seat setup started from one chat (Claude messaging or sequenced files for a Codex peer, including Codex + Codex)"
  default_prompt: "Start Pair-watch from this chat. If this is Codex and the user explicitly names a Codex peer, act as the Codex watcher and use the sequenced file transport. Otherwise preserve the Claude-watcher flow and wait for its pair-inbox."
