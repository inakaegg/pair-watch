interface:
  display_name: "Pair-watch"
  short_description: "Supervised coding from one chat with native Claude or Codex seats"
  default_prompt: "Start Pair-watch from this chat as the read-only watcher. A Codex watcher launches Codex implementers using the native seat procedure and settings. Use the legacy file-watch route only for an explicitly named user-opened Codex thread."
