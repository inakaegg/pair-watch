interface:
  display_name: "Pair-watch"
  short_description: "Two-seat setup started from one chat (role auto-detect; SendMessage for a Claude peer, file inbox/outbox + rollout audit for a Codex peer)"
  default_prompt: "Pair-watch is initiated from the Claude side. If invoked inside a Codex session, tell the user that the Claude watcher will deliver a pair-inbox file, stop, and follow that inbox's instructions once it is provided."
