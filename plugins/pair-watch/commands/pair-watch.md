---
description: Start a two-seat implementer + watcher setup from this chat
argument-hint: <one-line task>
---

Use the Skill tool to invoke the `pair-watch:pair-watch` skill, passing the arguments through
unchanged: $ARGUMENTS

If no arguments were given, ask the user for a one-line task first, then invoke the skill with it.
